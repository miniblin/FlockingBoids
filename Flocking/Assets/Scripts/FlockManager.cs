using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlockManager : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {
        //take 3d models, replicate them multiple times
    }

    // Update is called once per frame
    void Update()
    {
        /*       
        loop through models call relevant method (wander,seek etc) depending on models current state.
        or.. each model has its own update method and calls its own action method?
                
        */

    }





}